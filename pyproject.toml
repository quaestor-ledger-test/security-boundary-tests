[project]
name = "quaestor-ledger-test-security-boundary-tests"
version = "0.2.0"
description = "Tenant, replay, input, redaction, and GitHub Actions trust-boundary tests for Quaestor Ledger."
requires-python = ">=3.11"
dependencies = []

[tool.deep-tests]
suite = "security"
deterministic = true
network_default = "disabled"
